export interface ICreateTaxPayload {
  name: string;
  rate: string;
  purchase_account_id: string;
  sale_account_id: string;
  // email: string;
  // phone: string;
  // is_supplier: boolean;
  // is_customer: boolean;
}
